hi
x<-1
x
x+5
setwd("~/")
![Caption for the picture.](Pictures/xmas/mom.png)
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
knit_with_parameters('~/Christmas.Rmd')
knit_with_parameters('~/Christmas.Rmd')
knit_with_parameters('~/Christmas.Rmd')
setwd("~/Workspace/PITW/Posts/MLK")
install.packages("wordcloud")
install.packages("tm")
install.packages("tm")
install.packages("tm")
library(wordcloud)   # wordcloud generator
library(tm)          # text-mining
# read in our data
r.text <- readLines("ihad.txt")
# read in our data
r.text <- readLines("jail.txt")
library(wordcloud)   # wordcloud generator
library(tm)          # text-mining
# read in our data
r.text <- readLines("jail.txt")
# create a corpus objext from the text
r.corpus <- VCorpus(VectorSource(r.text))
# clean the words: whitespace, punctuation, remove numbeers, stem, stopwords
c.corpus <- tm_map(r.corpus, content_transformer(removePunctuation))
c.corpus <- tm_map(c.corpus, content_transformer(removeNumbers))
c.corpus <- tm_map(c.corpus, content_transformer(stripWhitespace))
c.corpus <- tm_map(c.corpus, removeWords, stopwords())
# set a palate
pal <-brewer.pal(8,"Dark2")
# create a wordcloud
wordcloud(c.corpus, min.freq = 2, random.order = FALSE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 10, random.order = FALSE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 5, random.order = FALSE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 7, random.order = FALSE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 6, random.order = FALSE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 8, random.order = FALSE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 8, random.order = TRUE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 7, random.order = TRUE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 5, random.order = TRUE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 6, random.order = TRUE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 7, random.order = TRUE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 7, random.order = FALSE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 6, random.order = FALSE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 7, random.order = FALSE, colors = pal)
# create a wordcloud
wordcloud(c.corpus, min.freq = 7, random.order = FALSE, colors = pal)
# set a palate
pal <-brewer.pal(8")
# set a palate
pal <-brewer.pal(8)
library(wordcloud)   # wordcloud generator
library(tm)          # text-mining
# read in our data
r.text <- readLines("jail.txt")
library(wordcloud)   # wordcloud generator
library(tm)          # text-mining
# read in our data
r.text <- readLines("jail.txt")
# create a corpus objext from the text
r.corpus <- VCorpus(VectorSource(r.text))
# clean the words: whitespace, punctuation, remove numbers, get root words, remove stopwords
c.corpus <- tm_map(r.corpus, content_transformer(removePunctuation))
c.corpus <- tm_map(c.corpus, content_transformer(removeNumbers))
c.corpus <- tm_map(c.corpus, content_transformer(stripWhitespace))
c.corpus <- tm_map(c.corpus, removeWords, stopwords())
# set a palate
pal <-brewer.pal(8)
library(wordcloud)   # wordcloud generator
library(tm)          # text-mining
# read in our data
r.text <- readLines("jail.txt")
# create a corpus objext from the text
r.corpus <- VCorpus(VectorSource(r.text))
# clean the words: whitespace, punctuation, remove numbers, get root words, remove stopwords
c.corpus <- tm_map(r.corpus, content_transformer(removePunctuation))
c.corpus <- tm_map(c.corpus, content_transformer(removeNumbers))
c.corpus <- tm_map(c.corpus, content_transformer(stripWhitespace))
c.corpus <- tm_map(c.corpus, removeWords, stopwords())
# set a palate
pal <-brewer.pal(8,"Dark2")
# create a wordcloud
wordcloud(c.corpus, min.freq = 7, random.order = FALSE, colors = pal)
